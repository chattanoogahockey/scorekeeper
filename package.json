{
  "name": "hockey-scorekeeper",
  "version": "2.1.0",
  "description": "Hockey scorekeeper application with automated Azure deployment",
  "type": "module",
  "private": true,
  "scripts": {
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "dev": "concurrently --names \"BACKEND,FRONTEND\" --colors \"blue,green\" \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "build": "echo 'Starting production build process...' && echo 'Installing frontend dependencies...' && cd frontend && npm install --include=dev && echo 'Frontend dependencies installed' && echo 'Building frontend application...' && npm run build && echo 'Frontend build complete!' && echo 'Verifying build output...' && dir dist && echo 'Installing backend dependencies...' && cd ../backend && npm install && echo 'Backend dependencies installed' && echo 'Production build completed successfully!'",
    "deploy:setup": "echo 'Follow DEPLOYMENT.md for Azure setup instructions'",
    "start": "node server.js",
    "postinstall": "echo 'Post-install complete'"
  },
  "keywords": [
    "hockey",
    "scorekeeper",
    "azure",
    "react",
    "express"
  ],
  "author": "Chattanooga Hockey",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "dotenv": "^17.2.1",
    "node-fetch": "^3.3.2"
  }
}
